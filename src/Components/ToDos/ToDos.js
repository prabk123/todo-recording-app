import React from "react";
import Title from "Shared/Title";

const ToDos = props => {
  return (
    <div style={{ height: "2000px" }}>
      <Title level={1}>This will be the todos page.</Title>
    </div>
  );
};

export default ToDos;
